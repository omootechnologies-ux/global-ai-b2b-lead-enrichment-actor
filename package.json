{
  "name": "global-ai-b2b-lead-enrichment-actor",
  "version": "0.1.0",
  "type": "module",
  "description": "Apify Actor that discovers, crawls, enriches, scores, and prepares outreach for B2B leads.",
  "main": "src/main.js",
  "scripts": {
    "start": "node src/main.js",
    "check": "find src -name \"*.js\" -print0 | xargs -0 -n1 node --check"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "apify": "^3.6.0",
    "crawlee": "^3.14.0"
  }
}
